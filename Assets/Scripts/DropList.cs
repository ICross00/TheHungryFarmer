using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class DropList : MonoBehaviour
{
    float[][] itemWeights;//2D Array mustr be properly decalred after the item weights have been decided.
    
    int GenerateItem()
    {
        return 0;//Int should be changed accordingly when item have been decided on.
    }
}
